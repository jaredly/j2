// Test cases are separated by double-newlines
1

toString#[hlmnop](1)

// before all
toWhat(
	// first
	1, // here we are
	inner(2, /* here */ 3), /* just chillin */
	1.000001,
	// last
	true
) // mid
// here
23

// type error in the argument
toString(@error(expected: :float, received: :bool) true)

(@hello() 10)(true)

// id<int>(1)

// () => 1

// (() => float#[b])(1)

// <T>(arg#[32]) => arg#[32]

// (<T#[12]>(arg) => arg)<T#[14], int#[b], float, whatsit#[habcd]>(23)

// this must be the ID function
// (f) => f((s) => s)(f("hi"))
